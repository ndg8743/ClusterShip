apiVersion: v1
kind: Pod
metadata:
  name: busybox-test
  labels:
    app: clustership
    service: test
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sh", "-c", "sleep 3600"]
    resources:
      limits:
        cpu: "10m"
        memory: "8Mi"
